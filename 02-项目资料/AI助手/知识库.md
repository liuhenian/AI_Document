
| type                     | 必须参数             | 可选参数                                                                                                           | 特殊                    |     |
| ------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------- | --------------------- | --- |
| 内存（process_mem_usage）    | ip 或 device_name | slot_id，device_id，label（进程名，整机时值为 total_using），project（项目）                                                     | 当 _score="ones"表示异常数据 |     |
| CPU（process_cpu_usage）   | ip 或 device_name | slot_id，device_id，label（进程名，整机时值为 total_using），project（项目）                                                     | 当 _score="ones"表示异常数据 |     |
| 文件IO （io_ebpf_file_size） | ip 或 device_name | device_id，slot_id，label（进程名），project（项目）                                                                       |                       |     |
| 磁盘使用（disk_usage）         | ip 或 device_name | slot_id，device_id，label（挂载目录），filesystem（挂载点存储来源），project（项目），type（如used）                                      | 当 _score="ones"表示异常数据 |     |
| 定时器频率（timer_count）       | ip 或 device_name | device_id，slot_id，label（定时器标识）                                                                                 |                       |     |
| ACL/FP 资源（timer_count）   | ip 或 device_name | device_id，slot_id，label（芯片逻辑单元如 slice1/slice2），res_type（资源类型: IFP/EFP/VFP），unit（芯片单元），val_type（total/used）     | 当 _score="ones"表示异常数据 |     |
| SCL 资源（acl_res）          | ip 或 device_name | device_id，slot_id，label（如“整机”），val_type（total/used），label（如“整机”）                                               |                       |     |
| mirror 资源（mirror_res）    | ip 或 device_name | device_id，slot_id，label（如“芯片0”），val_type（total/used），label（如“芯片0”），unit（芯片单元）                                  |                       |     |
| ARP 资源（fw_entry_res）     | ip 或 device_name | device_id，slot_id，label（如 IPV4/IPV4_32/IPV6_128/IPV6_64）val_type（total/used），unit，resource（L3_ENTRY/LPM/TOTAL） |                       |     |
| 路由资源（fw_obj_res）         | ip 或 device_name | device_id，slot_id，label（ECMP_GROUP/ENCP/NEXTHOP，val_type（total/used），unit（芯片单元）                                |                       |     |
| MAC 资源（l2_fw_res）        | ip 或 device_name | device_id，slot_id，label（如“整机”），val_type（total/used）                                                            |                       |     |
| VSL 带宽丢包（intf_bw_drop）   | ip 或 device_name | device_id，slot_id，label（端口名，如 HundredGigabitEthernet_1/3/17）dir（in/out），drop_type（dropped/no_buffer）           |                       |     |
| VSL 丢包率（intf_bw_usage）   | ip 或 device_name | device_id，slot_id，label（端口名），dir（in/out）                                                                       |                       |     |
| 健康度（healthy）             | ip 或 device_name | device_id，slot_id，                                                                                             |                       |     |
| 实体（entities）             |                  | user_name, ip,device_name, slot_id, device_id, interface                                                       |                       |     |
| 火焰图（profile）             | ip 或 device_name | device_id，slot_id，                                                                                             |                       |     |
| IO日志（IO_LOG）             | ip 或 device_name | device_id，slot_id，label （进程）                                                                                   |                       |     |
| malloc 日志（malloc_LOG）    | ip 或 device_name | device_id，slot_id，label （进程）                                                                                   |                       |     |
| 异常事件（abn_event）          | ip 或 device_name | device_id，slot_id                                                                                              |                       |     |
|                          |                  |                                                                                                                |                       |     |
